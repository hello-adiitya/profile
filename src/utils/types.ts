export interface FormData {
  name: string;
  email: string;
  phone: string;
  message: string;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface Achievement {
  icon: string;
  value: string;
  label: string;
  description: string;
}